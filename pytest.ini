[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts =
    --cov=utils
    --cov=models
    --cov=data
    --cov=analysis
    --cov=scoring
    --cov=reporting
    --cov-branch
    --cov-report=term-missing
    --cov-report=xml